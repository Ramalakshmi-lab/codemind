#include<bits/stdc++.h>
using namespace std;
int main()
{
    int n,s=0;
    cin>>n;
    int t=n;
    while(n>0)
    {
        s=s*10+(n%10);
        n=n/10;
    }
    if(s==t)
    cout<<"True";
    else
    cout<<"False";
}
