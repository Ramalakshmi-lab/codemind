#include<bits/stdc++.h>
using namespace std;
int getMax(int a[], int n)
{
    int r = a[0];
    for (int i = 1; i < n; i++)
        r = max(r,a[i]);
    return r;
}
int main()
{
    int n;
    cin>>n;
    int a[n];
    for(int i=0;i<n;i++)
    {
        cin>>a[i];
    }
    cout<<getMax(a,n);
}
