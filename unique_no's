#include<iostream>
using namespace std;
int main()
{
    int n,i,j,count=0,temp;
    cin>>n;
    int a[n];
    for(i=0;i<n;i++)
    {
        cin>>a[i];
    }
    for(i=0;i<n;i++)
    {
        for(j=i+1;j<n;j++)
        {
            if(a[i]!=temp)
            {
            if(a[i]==a[j])
            {
                a[j]=temp;
            }
            }
            
        }
        if(a[i]!=temp)
        cout<<a[i]<<" ";
        
    }
}
